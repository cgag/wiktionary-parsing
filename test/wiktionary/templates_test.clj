(ns wiktionary.templates-test
  (:require [clojure.test :refer :all]
            [wiktionary.templates-test]
            [clojure.core.reducers :as r]))

(comment (deftest templates
           (is )))
